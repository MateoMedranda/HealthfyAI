import 'package:flutter/material.dart';

class AppColors {
  // Colores principales basados en el logo de HealthfyAI
  static const Color primary = Color(0xFF4FBDBA); // Turquesa del logo
  static const Color primaryDark = Color(0xFF3A9D9A);
  static const Color primaryLight = Color(0xFF7FD5D2);

  // Colores de fondo
  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Colors.white;

  // Colores de texto
  static const Color textPrimary = Color(0xFF2C3E50);
  static const Color textSecondary = Color(0xFF7F8C8D);
  static const Color textHint = Color(0xFFBDC3C7);

  // Colores de estado
  static const Color error = Color(0xFFE74C3C);
  static const Color success = Color(0xFF27AE60);
  static const Color warning = Color(0xFFF39C12);

  // Colores adicionales
  static const Color white = Colors.white;
  static const Color black = Colors.black;
  static const Color grey = Color(0xFFECF0F1);
}
